native:
  macos-homebrew:
    packages:
      - fswatch
      - poppler
      - haskell-stack
      - rsync
      - ocrmypdf # for img2pdf
      #- imagemagick # for convert
      # brew install blogabe/homebrew-xplanet/imagemagick -s --with-pango
      - lftp
  debian:
    packages:
      - fswatch
      - poppler-utils
      - pdfsandwich
      - haskell-stack
      - rsync
      - img2pdf
      - imagemagick # for convert
      # backup
      - rsync          #  rsync
      - openssh-client #  ssh
      - curlftpfs      #  curlftpfs
      - lftp
  msys2-msys:
    packages:
      - lftp
      - rsync
      - openssh     # for SSH server
      - cygrunsrv   # for SSH server
  msys2-mingw64:
    packages:
      - mingw-w64-x86_64-editrights # for SSH server
